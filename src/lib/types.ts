export interface Post {
    title: string;
    language: string;
    link: string;
    tags: string[];
}

export interface Project {
    title: string;
    stack: string; 
    link: string;
    description: string;
    skills: string[]; 
    source: string;
}
